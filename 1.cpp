#define _USE_MATH_DEFINES
#include <cmath>
#include <iostream>

int main() {
	int n, k;
	int m, l;
	t = pow(10, 10);
	std::cin >> n >> k;
	for (int i = 0; i < n; i += 1) {
		std::cin >> m;

		if (k % m == 0) {
			if (k / m < l) {
				l = k / m;
			}

		}
	}
	std::cout << t;
}